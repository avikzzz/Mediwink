import { createTheme } from "@mui/material";

const Theme = createTheme({
    palette: {
        primary: {
            main: "#9c27b0", // purple
        },
        secondary: {
            main: "#00bcd4", // cyan
        },
    },
});

export default Theme;